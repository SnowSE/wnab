<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:WNAB.Maui.Behaviors"
             xmlns:local="clr-namespace:WNAB.Maui"
             x:Class="WNAB.Maui.CategoriesPage"
             x:DataType="local:CategoriesViewModel"
             Title="Categories">
    <ContentPage.Behaviors>
        <!-- LLM-Dev:v3 Use the shared OnAppearingOnceBehavior and correct LoadCommand name -->
        <behaviors:OnAppearingOnceBehavior Command="{Binding LoadCommand}" />
    </ContentPage.Behaviors>
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Category"
                     Priority="0"
                     Order="Primary"
                     Command="{Binding AddCategoryCommand}"
                     IsEnabled="{Binding IsLoggedIn}" />
    </ContentPage.ToolbarItems>
    
    <Grid Padding="16" RowDefinitions="Auto,Auto,*" RowSpacing="12">
        <!-- Status and refresh section -->
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8" VerticalOptions="Center">
            <Label Text="{Binding StatusMessage}" VerticalOptions="Center" />
            <Button Grid.Column="1" Text="Refresh" Command="{Binding RefreshCommand}" />
        </Grid>

        <!-- Login prompt when not logged in -->
        <Label Grid.Row="1" Text="Please use the Login page to sign in first." 
               IsVisible="{Binding IsLoggedIn, Converter={StaticResource InvertedBoolConverter}}"
               HorizontalOptions="Center" 
               TextColor="Orange"
               Margin="0,10" />
        
        <ActivityIndicator Grid.Row="2" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        <CollectionView Grid.Row="2" ItemsSource="{Binding Categories}" IsVisible="{Binding IsLoggedIn}">
            <CollectionView.EmptyView>
                <Label Text="No categories yet." HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:CategoryItem">
                    <HorizontalStackLayout Padding="8,6">
                        <Label Text="{Binding Name}" FontSize="16" />
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
