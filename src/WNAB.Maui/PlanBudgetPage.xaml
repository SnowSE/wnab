<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WNAB.Maui.PlanBudgetPage"
             Title="Plan Budget">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Home"
                     Priority="1"
                     Order="Primary"
                     Command="{Binding NavigateToHomeCommand}" />
    </ContentPage.ToolbarItems>
    <!-- LLM-Dev: Grid layout with categories column on left, content area on right, and bottom toolbar -->
    <Grid ColumnDefinitions="250,*" RowDefinitions="*,Auto">
        
        <!-- LLM-Dev: Left column - Categories list following MVVM pattern -->
        <Border Grid.Column="0" 
                Grid.Row="0"
                Padding="10"
                BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                StrokeThickness="0">
            <VerticalStackLayout Spacing="10">
                <Label Text="Categories" 
                       FontSize="20" 
                       FontAttributes="Bold"
                       Margin="0,0,0,10"/>
                
                <CollectionView ItemsSource="{Binding Categories}"
                                SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8"
                                    Margin="0,2"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                                    StrokeThickness="0">
                                <Label Text="{Binding Name}" 
                                       FontSize="16"/>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                
                <!-- LLM-Dev: Status message for loading/error feedback -->
                <Label Text="{Binding StatusMessage}" 
                       FontSize="12"
                       IsVisible="{Binding IsBusy}"
                       Margin="0,10,0,0"/>
            </VerticalStackLayout>
        </Border>
        
        <!-- LLM-Dev: Right column - Budget planning content area -->
        <VerticalStackLayout Grid.Column="1"
                            Grid.Row="0"
                            Padding="30,0"
                            Spacing="25"
                            VerticalOptions="Center"
                            HorizontalOptions="Center">
            
            <Label Text="Monthly budget here"
                   Style="{StaticResource Headline}"
                   HorizontalOptions="Center" />
                
        </VerticalStackLayout>
        
        <!-- LLM-Dev: Bottom toolbar with Save and Cancel buttons -->
        <Border Grid.Row="1" 
                Grid.ColumnSpan="2"
                Padding="20,10"
                BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}"
                StrokeThickness="0">
            <HorizontalStackLayout Spacing="10" 
                                   HorizontalOptions="End">
                <Button Text="Cancel" 
                        WidthRequest="100"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray700}}" />
                <Button Text="Save" 
                        WidthRequest="100"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray700}}" />
            </HorizontalStackLayout>
        </Border>
    </Grid>
</ContentPage>
