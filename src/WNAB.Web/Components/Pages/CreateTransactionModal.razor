@using WNAB.MVM
@inject AddTransactionViewModel ViewModel
@inject IJSRuntime JSRuntime

<!-- Create Transaction Modal -->
<div class="modal fade" id="createTransactionModal" tabindex="-1" aria-labelledby="createTransactionModalLabel" aria-hidden="true">
	<div class="modal-dialog modal-lg">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="createTransactionModalLabel">Create Transaction</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
			</div>
			<EditForm Model="ViewModel.Model" OnValidSubmit="@HandleSave">
				<div class="modal-body">
					@if (!string.IsNullOrWhiteSpace(ViewModel.Model.StatusMessage))
					{
						<div class="alert @GetAlertClass(ViewModel.Model.StatusMessage)">@ViewModel.Model.StatusMessage</div>
					}

					<div class="row">
						<div class="col-md-6">
							<div class="mb-3">
								<label class="form-label">Account</label>
								<select class="form-select" @bind="ViewModel.Model.AccountId" required>
									<option value="0">Select an account...</option>
									@foreach (var account in ViewModel.Model.AvailableAccounts)
									{
										<option value="@account.Id">@account.AccountName</option>
									}
								</select>
							</div>
						</div>
						<div class="col-md-6">
							<div class="mb-3">
								<label class="form-label">Transaction Date</label>
								<input type="date" class="form-control" @bind="ViewModel.Model.TransactionDate" required />
							</div>
						</div>
					</div>

					<div class="row">
						<div class="col-md-6">
							<div class="mb-3">
								<label class="form-label">Payee</label>
								<input class="form-control" @bind="ViewModel.Model.Payee" required />
							</div>
						</div>
						<div class="col-md-6">
							<div class="mb-3">
								<label class="form-label">Amount</label>
								<input type="number" step="0.01" class="form-control" @bind="ViewModel.Model.Amount" required />
							</div>
						</div>
					</div>

					<div class="mb-3">
						<div class="d-flex align-items-center gap-2 mb-2">
							<label class="form-label mb-0">Category</label>
							<button type="button" class="btn btn-sm btn-outline-primary" @onclick="ViewModel.ToggleSplitTransactionCommand.Execute">
								@(ViewModel.Model.IsSplitTransaction ? "Single Category" : "Split Transaction")
							</button>
						</div>

						@if (!ViewModel.Model.IsSplitTransaction)
						{
							<select class="form-select" @bind="ViewModel.Model.CategoryId" required>
								<option value="0">Select a category...</option>
								@foreach (var category in ViewModel.Model.AvailableCategories)
								{
									<option value="@category.Id">@category.Name</option>
								}
							</select>
						}
						else
						{
							<div class="border rounded p-3 bg-light">
								@foreach (var split in ViewModel.Model.Splits)
								{
									<div class="row g-2 mb-2 align-items-center">
										<div class="col-md-6">
											<InputSelect class="form-select form-select-sm" @bind-Value="split.Model.SelectedCategory">
												<option value="">Select category...</option>
												@foreach (var category in ViewModel.Model.AvailableCategories)
												{
													<option value="@category">@category.Name</option>
												}
											</InputSelect>
										</div>
										<div class="col-md-4">
											<input type="number" step="0.01" class="form-control form-control-sm"
												   @bind="split.Model.Amount" placeholder="0.00" />
										</div>
										<div class="col-md-2">
											<button type="button" class="btn btn-sm btn-danger w-100"
													@onclick="() => ViewModel.RemoveSplitCommand.Execute(split)"
													disabled="@(ViewModel.Model.Splits.Count <= 1)">
												✕
											</button>
										</div>
									</div>
								}

								<button type="button" class="btn btn-sm btn-success mb-2" @onclick="ViewModel.AddSplitCommand.Execute">
									+ Add Split
								</button>

								<div class="d-flex align-items-center gap-2 mt-2">
									<small class="text-muted">Balance:</small>
									<small class="fw-bold @(ViewModel.Model.AreSplitsBalanced ? "text-success" : "text-danger")">
										@ViewModel.Model.RemainingAmount.ToString("C")
									</small>
									<small class="text-muted">remaining</small>
								</div>
							</div>
						}
					</div>

					<div class="mb-3">
						<label class="form-label">Memo (Optional)</label>
						<textarea class="form-control" rows="2" @bind="ViewModel.Model.Memo"></textarea>
					</div>
				</div>
				<div class="modal-footer">
					<button type="button" class="btn btn-secondary" data-bs-dismiss="modal" @onclick="ViewModel.CancelCommand.Execute">Cancel</button>
					<button class="btn btn-primary" type="submit" disabled="@ViewModel.Model.IsBusy">
						@if (ViewModel.Model.IsBusy)
						{
							<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
						}
						Create Transaction
					</button>
				</div>
			</EditForm>
		</div>
	</div>
</div>

@code {
	[Parameter] public EventCallback OnTransactionCreated { get; set; }

	protected override async Task OnInitializedAsync()
	{
		await ViewModel.InitializeAsync();
	}

	private async Task HandleSave()
	{
		await ViewModel.SaveCommand.ExecuteAsync(null);

		// Notify parent to refresh list
		await OnTransactionCreated.InvokeAsync();

		// Close modal via JavaScript
		await CloseModalAsync();
	}

	private async Task CloseModalAsync()
	{
		await JSRuntime.InvokeVoidAsync("eval", "bootstrap.Modal.getInstance(document.getElementById('createTransactionModal'))?.hide()");
	}

	private string GetAlertClass(string message)
	{
		if (message.Contains("Error") || message.Contains("Please") || message.Contains("Missing") || message.Contains("No "))
			return "alert-danger";
		if (message.Contains("success"))
			return "alert-success";
		return "alert-info";
	}
}