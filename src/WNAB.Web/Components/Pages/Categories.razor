@page "/categories"
@inject CategoriesViewModel ViewModel
@implements IDisposable

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1>Categories</h1>
    </div>
    <div>
        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createCategoryModal" @onclick="OnAddCategoryClick">
            <i class="bi bi-plus-circle me-1"></i>
            Create Category
        </button>
        <button class="btn btn-outline-secondary ms-2" @onclick="OnRefreshClick" disabled="@ViewModel.Model.IsBusy">
            @if (ViewModel.Model.IsBusy)
            {
                <span class="spinner-border spinner-border-sm me-1" role="status" aria-hidden="true"></span>
            }
            else
            {
                <i class="bi bi-arrow-clockwise me-1"></i>
            }
            Refresh
        </button>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(ViewModel.Model.StatusMessage))
{
    <div class="alert alert-info">@ViewModel.Model.StatusMessage</div>
}

@if (!ViewModel.Model.IsLoggedIn)
{
    <div class="alert alert-warning">
        <i class="bi bi-exclamation-triangle me-1"></i>
        Please log in to view categories.
    </div>
}

<div class="responsive-panel">
    @if (ViewModel.Model.Items == null)
    {
        <div class="text-center p-4">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-2 text-muted">Loading categories...</p>
        </div>
    }
    else if (ViewModel.Model.Items.Count == 0)
    {
        <div class="text-center p-4">
            <i class="bi bi-tags fs-1 text-muted"></i>
            <p class="mt-2">No categories found. Click "Create Category" to add your first category.</p>
        </div>
    }
    else
    {
        <div class="category-grid">
            @foreach (var c in ViewModel.Model.Items)
            {
                <div class="category-card">
                    <div class="category-card-header d-flex align-items-start justify-content-between">
                        <div class="d-flex align-items-center gap-3">
                            <div class="category-dot" style="background-color:@(string.IsNullOrWhiteSpace(c.Color) ? "#E53E3E" : c.Color)"></div>
                            <div class="fw-semibold">@c.Name</div>
                        </div>
                        <div class="category-actions">
                            <button class="btn btn-link btn-sm text-dark p-1" title="Edit" @onclick="() => EditCategory(c.Id)">
                                <i class="bi bi-pencil"></i>
                            </button>
                            <button class="btn btn-link btn-sm text-danger p-1" title="Delete" @onclick="() => DeleteCategory(c.Id)">
                                <i class="bi bi-trash"></i>
                            </button>
                        </div>
                    </div>

                    <div class="mt-3">
                        <span class="badge bg-light text-secondary small">@(c.IsIncome ? "Income" : "Expense")</span>
                    </div>

                    <div class="category-card-body mt-4">
                        <div class="category-amount">$0.00</div>
                        <div class="text-muted small">Total spent</div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<!-- Include the Create Category Modal -->
<CreateCategoryModal OnCategoryCreated="OnCategoryCreated" />

@code {
    // LLM-Dev:v6 Refactored to use WNAB.MVM ViewModels and Models for shared business logic
    // UI is now a thin presentation layer, all logic delegated to ViewModel/Model
    
    protected override async Task OnInitializedAsync()
    {
        // Subscribe to property changes to trigger UI updates
        ViewModel.Model.PropertyChanged += OnModelPropertyChanged;
        
        await ViewModel.InitializeAsync();
    }

    private void OnModelPropertyChanged(object? sender, System.ComponentModel.PropertyChangedEventArgs e)
    {
        // When Model properties change, update the UI
        InvokeAsync(StateHasChanged);
    }

    private async Task OnRefreshClick()
    {
        await ViewModel.RefreshCommand.ExecuteAsync(null);
    }

    private async Task OnAddCategoryClick()
    {
        // Modal is shown via Bootstrap, but we call the ViewModel command for consistency
        await ViewModel.AddCategoryCommand.ExecuteAsync(null);
    }

    private async Task OnCategoryCreated()
    {
        // Called by modal when category is created - refresh the list
        await ViewModel.Model.RefreshAsync();
    }

    // Placeholder action handlers
    private void EditCategory(int categoryId)
    {
        // Future: open edit modal
    }

    private async Task DeleteCategory(int categoryId)
    {
        // Future: implement delete with confirmation
    }

    public void Dispose()
    {
        if (ViewModel?.Model != null)
        {
            ViewModel.Model.PropertyChanged -= OnModelPropertyChanged;
        }
    }
}