@page "/categories"
@inject CategoriesViewModel ViewModel
@inject CategoryManagementService CategoryService
@inject WNAB.MVM.IAuthenticationService AuthService
@inject NavigationManager Navigation
@inject IJSRuntime JS
@implements IDisposable

<div class="page-container">
    <div class="accounts-header">
        <div class="header-text">
            <h1 class="page-title">Categories</h1>
        </div>
        <div class="header-actions">
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createCategoryModal" @onclick="OnAddCategoryClick">
                <i class="bi bi-plus-circle me-1"></i>
                Create Category
            </button>
            <button class="btn btn-secondary" @onclick="OnRefreshClick" disabled="@ViewModel.Model.IsBusy">
                @if (ViewModel.Model.IsBusy)
                {
                    <span class="spinner-border spinner-border-sm me-1" role="status" aria-hidden="true"></span>
                }
                else
                {
                    <i class="bi bi-arrow-clockwise me-1"></i>
                }
                Refresh
            </button>
        </div>
    </div>

@if (!ViewModel.Model.IsLoggedIn)
{
    <div class="alert alert-warning">
        <i class="bi bi-exclamation-triangle me-1"></i>
        Please log in to view categories.
    </div>
}

    @if (ViewModel.Model.Items == null)
    {
        <div class="text-center p-4">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-2 text-muted">Loading categories...</p>
        </div>
    }
    else if (ViewModel.Model.Items.Count == 0)
    {
        <div class="text-center p-4">
            <i class="bi bi-tags fs-1 text-muted"></i>
            <p class="mt-2">No categories found. Click "Create Category" to add your first category.</p>
        </div>
    }
    else
    {
        <div class="category-grid">
            @foreach (var c in ViewModel.Model.Items)
            {
                <div class="category-card">
                    <div class="category-card-header d-flex align-items-start justify-content-between">
                        <div class="d-flex align-items-center gap-3">
                            <div class="category-dot" style="background-color:@(string.IsNullOrWhiteSpace(c.Color) ? "#E53E3E" : c.Color)"></div>
                            <div class="fw-semibold">@c.Name</div>
                        </div>
                        <div class="category-actions">
                            <button class="btn btn-link btn-sm text-dark p-1" title="Edit" @onclick="() => EditCategory(c)">
                                <i class="bi bi-pencil"></i>
                            </button>
                            <button class="btn btn-link btn-sm text-danger p-1" title="Delete" @onclick="() => DeleteCategory(c)">
                                <i class="bi bi-trash"></i>
                            </button>
                        </div>
                    </div>

                    <div class="category-card-body mt-4">
                        <div class="category-amount">$0.00</div>
                        <div class="text-muted small">Total spent</div>
                    </div>
                </div>
            }
        </div>
    }

<!-- Include the Create Category Modal -->
<CreateCategoryModal OnCategoryCreated="OnCategoryCreated" />

<!-- Include the Edit Category Modal -->
<EditCategoryModal @ref="editCategoryModal" OnCategoryUpdated="OnCategoryUpdated" />

@code {
    // LLM-Dev:v6 Refactored to use WNAB.MVM ViewModels and Models for shared business logic
    // UI is now a thin presentation layer, all logic delegated to ViewModel/Model
    
    private EditCategoryModal? editCategoryModal;
    
    protected override async Task OnInitializedAsync()
    {
        // Check if user is authenticated and token is valid
        if (AuthService is WNAB.Web.Services.WebAuthenticationService webAuthService)
        {
            var isAuthenticated = await AuthService.IsAuthenticatedAsync();
            var isTokenExpired = await webAuthService.IsTokenExpiredAsync();

            if (!isAuthenticated || isTokenExpired)
            {
                Navigation.NavigateTo("/landing");
                return;
            }
        }

        // Subscribe to property changes to trigger UI updates
        ViewModel.Model.PropertyChanged += OnModelPropertyChanged;
        
        await ViewModel.InitializeAsync();
    }

    private void OnModelPropertyChanged(object? sender, System.ComponentModel.PropertyChangedEventArgs e)
    {
        // When Model properties change, update the UI
        InvokeAsync(StateHasChanged);
    }

    private async Task OnRefreshClick()
    {
        await ViewModel.RefreshCommand.ExecuteAsync(null);
    }

    private async Task OnAddCategoryClick()
    {
        // Modal is shown via Bootstrap, but we call the ViewModel command for consistency
        await ViewModel.AddCategoryCommand.ExecuteAsync(null);
    }

    private async Task OnCategoryCreated()
    {
        // Called by modal when category is created - refresh the list
        await ViewModel.Model.RefreshAsync();
    }

    private async Task OnCategoryUpdated()
    {
        // Called by modal when category is updated - refresh the list and close modal
        await ViewModel.Model.RefreshAsync();
        await JS.InvokeVoidAsync("eval", "bootstrap.Modal.getInstance(document.getElementById('editCategoryModal'))?.hide()");
    }

    private async Task EditCategory(Category category)
    {
        if (category == null) return;

        editCategoryModal?.Initialize(category.Id, category.Name, category.Color, category.IsActive);
        await JS.InvokeVoidAsync("eval", "new bootstrap.Modal(document.getElementById('editCategoryModal')).show()");
    }

    private async Task DeleteCategory(Category category)
    {
        if (category == null) return;

        var confirmed = await JS.InvokeAsync<bool>("confirm", $"Are you sure you want to delete '{category.Name}'? This action cannot be undone.");
        if (!confirmed) return;

        try
        {
            await CategoryService.DeleteCategoryAsync(category.Id);
            await ViewModel.Model.RefreshAsync();
        }
        catch (Exception ex)
        {
            await JS.InvokeVoidAsync("alert", $"Failed to delete category: {ex.Message}");
        }
    }

    public void Dispose()
    {
        if (ViewModel?.Model != null)
        {
            ViewModel.Model.PropertyChanged -= OnModelPropertyChanged;
        }
    }
}
</div>